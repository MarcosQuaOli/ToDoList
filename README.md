Projeto To Do List

Primeiro deve criar um banco de dados Mysql com o nome vue_teste.

Para testar a aplicação bastar rodar os codigos abaixo:

-npm install
-composer install
-npm run dev
-php artisan migrate
-php artisan serve
